# Memo 2025-12-06

Although now that I know how to use GitHub Actions for simple scripts,
I cannot yet utilize it for CI/CD pipelines,
I have ensured that I can execute simple scripts on remote Ubuntu environments.
However, I totally lack knowledge of what goes on under the hood.

In [official documentation](https://docs.github.com/en/actions/get-started/understand-github-actions):
> GitHub provides Ubuntu Linux, Microsoft Windows, and macOS runners to run your workflows.
> Each workflows run executes in a fresh, newly-provisioned virtual machine.

We may enable to specify which virtual machine to use, in `.github/workflows/some_workflow.yml`:

```yaml
name: Example Workflow
on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
```

Now I have a question:
